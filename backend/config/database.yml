default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  username: vlad

development:
  <<: *default
  database: eshop_development

test:
  <<: *default
  database: eshop_test

production:
  <<: *default
  url: <%= ENV['DATABASE_URL'] %>
